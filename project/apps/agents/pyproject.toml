[project]
name = "agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.12",
    "langgraph>=1.2.8",
    "pyspice>=1.5",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
